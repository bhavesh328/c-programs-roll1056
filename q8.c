//  calculate a perimeter of a square  in c programing //
#include<stdio.h>
  int main()
  {
    float side, perimeter;
      printf("enter a length of the side of the square");
      scanf("%f",&side);
       perimeter=4*side;
       printf("the perimeter of the square is: %.2f\n ", perimeter);
        return 0;
        }
        
